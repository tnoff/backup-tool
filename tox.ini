[tox]
min_version = 4.0
env_list =
    py311
    py312
    py313
    py314

[testenv]
extras = test
commands =
    pylint backup_tool
    bandit -r backup_tool
    pytest --cov=backup_tool --cov-report=term --cov-report=xml --cov-fail-under=85 tests/
